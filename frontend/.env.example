# Vite only exposes variables prefixed VITE_ to the browser bundle. Anything
# else stays server-side, which is the point — a secret named without the
# prefix is a secret that stays out of the JavaScript everyone can read.
VITE_API_URL=http://localhost:8000
