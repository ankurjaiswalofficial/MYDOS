# Read by app/settings.py through pydantic-settings. Every variable is prefixed
# APP_ so a stray HOST or DEBUG in the shell cannot reconfigure this service.
APP_NAME=MYDOS
APP_DEBUG=1
APP_CORS_ORIGINS=["*"]
APP_DATABASE_URL=sqlite+aiosqlite:///./mydos.db
